#include "main.h"
#include <stdio.h>
/**
 * _putchar - writes the character c to stdout
 * @ch: the character argument
 *
 * Return: the character to be printed
 *
 */
int _putchar(char ch)
{
	return (putchar(ch));
}

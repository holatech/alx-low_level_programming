#include <stdio.h>

/**
 * * main - Entry point for the prpgtamm.
 * *
 * * Return: 0
 * */

int main(void)
{
		int a = '0';

			for (a = '0'; a <= '9'; a++)
					{
								putchar(a);
									}
				putchar('\n');
					return (0);
}
